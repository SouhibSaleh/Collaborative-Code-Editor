# Step 1: Use an official Node.js image as a parent image
FROM node:23-slim

# Step 2: Set the working directory inside the container
WORKDIR /app

# Step 3: Copy the package.json and package-lock.json (or yarn.lock)
# This allows Docker to cache npm install layers to improve build times
COPY package*.json ./

# Step 4: Install dependencies
RUN npm install

# Step 5: Copy the rest of the project into the container
COPY . .

# Step 6: Build the Svelte project with Vite
RUN npm run build

# Step 7: Expose the port that the app will run on
EXPOSE 5173

# Step 8: Start the Svelte app using Vite's preview command
CMD ["npm", "run", "preview", "--", "--port", "5173"]
